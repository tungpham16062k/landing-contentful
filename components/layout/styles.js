const { makeStyles } = require('@mui/styles');

export const useStyle = makeStyles(theme => ({
    wrapper: {
        width: '100vw',
        height: '100vh',
        overflow: 'hidden auto',
    },
}));